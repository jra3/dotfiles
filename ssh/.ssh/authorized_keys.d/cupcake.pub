# cupcake — YubiKey 5 Nano, serial 37196474
# Public halves of resident FIDO2 credentials. Safe to commit.
#
# MISSING: cupcake's auth key (application `ssh:github-cupcake`) was not in
# MiniMini's authorized_keys, so it could not be captured here. On cupcake run:
#     ssh-keygen -lf ~/.ssh/id_ed25519_sk.pub   # confirm it is ssh:github-cupcake
#     cat ~/.ssh/id_ed25519_sk.pub
# and paste the line below, then re-run build-authorized-keys on every host.
